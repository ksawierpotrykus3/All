# Package marker for vinted/testy_camoufox/tools — diagnostic helpers, harvesters, and probes.
# Convention: tools with leading underscore (_*.py) are standalone probes; tools without
# underscore are canonical, reusable utilities (e.g. manage_documentation_conflicts.py).
# See docs/synthesis/ for tool provenance and [UDOWODNIONE] status of each.
